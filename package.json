{
  "name": "9router-for-github-copilot",
  "displayName": "9Router for Github Copilot (OpenAI compatible)",
  "description": "Connect GitHub Copilot to 9Router and any OpenAI-compatible server for open-source models",
  "version": "2.0.0",
  "publisher": "hotrungnhan",
  "icon": "assets/icon.png",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/hotrungnhan/9router-for-github-copilot.git"
  },
  "engines": {
    "vscode": "^1.125.0"
  },
  "packageManager": "bun",
  "scripts": {
    "vscode:prepublish": "bun run build:prod",
    "base": "esbuild ./src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node --target=es2020",
    "build": "bun run base -- --sourcemap",
    "build:prod": "bun run base -- --minify",
    "build:watch": "bun run base -- --sourcemap --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "test:compile": "tsc -p ./",
    "test:clean": "rm -rf out-test",
    "test:build": "bun run test:clean && tsc -p tsconfig.test.json",
    "test:run": "node --test \"out-test/**/*.test.js\"",
    "test": "bun run test:build && bun run test:run",
    "test:coverage": "bun run test:build && bun run test:run -- --experimental-test-coverage",
    "package": "vsce package --no-yarn",
    "deploy": "vsce publish --no-yarn",
    "deploy:pre-release": "vsce publish --pre-release --no-yarn",
    "install:local": "code --install-extension ./9router-for-github-copilot-*.vsix --force"
  },
  "categories": [
    "AI",
    "Machine Learning",
    "Chat",
    "Programming Languages"
  ],
  "keywords": [
    "copilot",
    "vllm",
    "Ollama",
    "llama.cpp",
    "LocalAI"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languageModelChatProviders": [
      {
        "vendor": "9router-github-copilot",
        "displayName": "9Router",
        "managementCommand": "9router-for-github-copilot.manage"
      }
    ],
    "commands": [
      {
        "command": "9router-for-github-copilot.manage",
        "title": "Manage Providers",
        "category": "9Router"
      },
      {
        "command": "9router-for-github-copilot.addProvider",
        "title": "Add Provider",
        "category": "9Router"
      },
      {
        "command": "9router-for-github-copilot.testConnection",
        "title": "Test Server Connection",
        "category": "9Router"
      },
      {
        "command": "9router-for-github-copilot.refreshModels",
        "title": "Refresh Models",
        "category": "9Router"
      },
      {
        "command": "9router-for-github-copilot.editCustomHeaders",
        "title": "Edit Custom Headers",
        "category": "9Router"
      },
      {
        "command": "9router-for-github-copilot.selectInlineCompletionModel",
        "title": "Select Inline Completion Model",
        "category": "9Router"
      },
      {
        "command": "9router-for-github-copilot.showOutput",
        "title": "Show Output Log",
        "category": "9Router"
      }
    ],
    "configuration": {
      "title": "9Router",
      "properties": {
        "9router-for-github-copilot.requestTimeout": {
          "type": "number",
          "default": 60000,
          "description": "Request timeout in milliseconds",
          "order": 1
        },
        "9router-for-github-copilot.defaultMaxTokens": {
          "type": "number",
          "default": 262144,
          "description": "Fallback context window size (input tokens) when the server does not report it",
          "order": 2
        },
        "9router-for-github-copilot.defaultMaxOutputTokens": {
          "type": "number",
          "default": 4096,
          "description": "Fallback maximum output tokens when the server does not report context size",
          "order": 3
        },
        "9router-for-github-copilot.enableImageInput": {
          "type": "boolean",
          "default": true,
          "description": "Enable image input capability for models that support multimodal requests",
          "order": 4
        },
        "9router-for-github-copilot.enableToolCalling": {
          "type": "boolean",
          "default": true,
          "description": "Enable tool calling capability for models",
          "order": 5
        },
        "9router-for-github-copilot.parallelToolCalling": {
          "type": "boolean",
          "default": true,
          "description": "Allow model to call multiple tools in parallel (disable if model has issues with parallel calls)",
          "order": 6
        },
        "9router-for-github-copilot.agentTemperature": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "maximum": 2,
          "description": "Fallback temperature for agent/tool calling mode (lower = more consistent tool call formatting). Set to 0.0 for best results with fine-tuned models. A temperature discovered from the backend's own model config (e.g. an Ollama Modelfile via /api/show) takes precedence; use perModelOptions to force a specific value.",
          "order": 7
        },
        "9router-for-github-copilot.verboseLogging": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, the full chat request body (including messages and tool args) is written to the output channel. Keep disabled unless you are reproducing an issue — captured logs may contain conversation content from the user's current editor.",
          "order": 8
        },
        "9router-for-github-copilot.extraModelOptions": {
          "type": "object",
          "default": {},
          "additionalProperties": true,
          "description": "Extra parameters merged into the chat-completions request body (e.g. top_k, repetition_penalty, seed). Per-request modelOptions from the caller take precedence. Edit in settings.json for non-string values.",
          "order": 9
        },
        "9router-for-github-copilot.perModelOptions": {
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "object",
            "additionalProperties": true
          },
          "description": "Per-model chat-completion parameters, keyed by model id. Merged on top of 'extraModelOptions' for matching models; per-request modelOptions from the caller still take precedence. Keys match the model id exactly, or use a '*' wildcard for a whole family (case-insensitive, e.g. 'qwen*'). Exact-id entries win over wildcard entries. Example: { \"qwen*\": { \"temperature\": 0.7, \"top_p\": 0.8 }, \"deepseek-r1\": { \"temperature\": 0.6 } }. Edit in settings.json.",
          "order": 10
        },
        "9router-for-github-copilot.modelContextWindows": {
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "number"
          },
          "markdownDescription": "Per-model context window override (total tokens), keyed by model id. Wins over what the server reports. Use when the server reports no context size — or the wrong one (e.g. llama-server router mode advertises nothing until a model is loaded). Keys match the model id exactly, or use a `*` wildcard (case-insensitive); exact-id entries win. Example: `{ \"qwen2.5-coder-32b\": 32768, \"llama*\": 123904 }`. Edit in settings.json.",
          "order": 11
        },
        "9router-for-github-copilot.enableInlineCompletion": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Experimental. Provide inline (ghost-text) code completions from your inference server, running **alongside** GitHub Copilot (not through it — VS Code does not expose bring-your-own-key models to its own inline suggestions). Requires a model/server that supports fill-in-the-middle via the `/v1/completions` `suffix` parameter (vLLM, llama.cpp, LM Studio, …). Leave disabled if you already use Copilot completions, to avoid two suggestions competing.",
          "order": 12
        },
        "9router-for-github-copilot.inlineCompletionProvider": {
          "type": "string",
          "default": "",
          "description": "Provider profile ID to route inline completions to. Leave blank to use the first enabled provider profile.",
          "order": 13
        },
        "9router-for-github-copilot.inlineCompletionModel": {
          "type": "string",
          "default": "",
          "description": "Model id to use for inline completions. Leave blank to use the first model reported by the server. Tip: point this at a small fill-in-the-middle / base model (e.g. a *-base or code FIM model) rather than a chat model for faster, cleaner completions.",
          "order": 14
        },
        "9router-for-github-copilot.inlineCompletionMaxTokens": {
          "type": "number",
          "default": 256,
          "minimum": 1,
          "description": "Maximum tokens generated per inline completion. Lower values are faster and cheaper.",
          "order": 15
        },
        "9router-for-github-copilot.inlineCompletionDebounce": {
          "type": "number",
          "default": 300,
          "minimum": 0,
          "description": "Milliseconds to wait after the last keystroke before requesting an inline completion. Higher values reduce load on the inference server while typing.",
          "order": 16
        },
        "9router-for-github-copilot.inlineCompletionTimeout": {
          "type": "number",
          "default": 3000,
          "minimum": 1,
          "description": "Per-request timeout (milliseconds) for inline completions. Kept short so a slow server doesn't block ghost-text suggestions.",
          "order": 17
        },
        "9router-for-github-copilot.inlineCompletionMaxPrefixChars": {
          "type": "number",
          "default": 4000,
          "minimum": 1,
          "description": "Maximum characters of context before the cursor to send for inline completions. Lower values reduce latency but provide less context.",
          "order": 18
        },
        "9router-for-github-copilot.inlineCompletionMaxSuffixChars": {
          "type": "number",
          "default": 1000,
          "minimum": 1,
          "description": "Maximum characters of context after the cursor to send for inline completions. Lower values reduce latency but provide less context.",
          "order": 19
        }
      }
    }
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/vscode": "^1.125.0",
    "@typescript-eslint/eslint-plugin": "^8.65.0",
    "@typescript-eslint/parser": "^8.65.0",
    "@typescript/native": "npm:typescript@^7.0.2",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "eslint": "^10.8.0",
    "typescript": "npm:@typescript/typescript6@^6.0.2"
  },
  "trustedDependencies": [
    "@vscode/vsce-sign"
  ]
}